package com.ancient.nedaire.util.helpers;

public class MultiblockHelper 
{
/*	public static boolean isRightPlacement (TileEntity tile)
	{
		return isMultiblock(tile) && NedaireMultiblocks.multiblocks.get(tile.getType().getRegistryName().getPath()).checkPos(tile);
	}
	
	public static boolean isRightPlacement (World world, BlockPos pos)
	{
		TileEntity tile = BlockHelper.getTileEntity(world, pos);
		if (tile != null && isMultiblock(tile))
		{
			return isRightPlacement(tile);
		}
		return false;
	}
	
	public static boolean isMultiblock (TileEntity tile)
	{
		return NedaireMultiblocks.multiblocks.containsKey(tile.getType().getRegistryName().getPath());
	}
	
	public static boolean isMultiblock (World world, BlockPos pos)
	{
		TileEntity tile = BlockHelper.getTileEntity(world, pos);
		if (tile != null)
		{
			return isMultiblock(tile);
		}
		return false;
	}
*/}
